<a href="{{ link.href }}" id="{{ id }}" class="button -{{ type }} {{ class }}" title="{{ link.title }}" target="{{ link.target }}" aria-label="{{ link.ariaLabel }}" rel="{{ link.rel }}">
	{{ link.text }}
</a>
